<li id="<%= feed_item.id %>">
	<% # feed func in the user model returns a micropost array.
	   # so each feed_item is a micropost. it has a user_id and a content.
	   # using belongs_to association, micropost.user returns the user
	   # whose user_id is in the micropost, automatically.
	 %>
  <%= link_to gravatar_for(feed_item.user), feed_item.user %>
  <span class="user">
    <%= link_to feed_item.user.name, feed_item.user %>
  </span>
  <span class="content"><%= feed_item.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
  </span>
  <%= render 'shared/delete_link', object: feed_item  %>
  
</li>